[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm install -g pnpm@8 && pnpm install"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run preview --host 0.0.0.0 --port $PORT"
